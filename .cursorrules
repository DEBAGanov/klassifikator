# Cursor Rules для проекта Klassifikator

## Общие правила разработки

### Язык коммуникации
- Всегда отвечай на русском языке
- Комментарии в коде на английском
- Документация на русском

### Принципы разработки
- Следуй принципам ООП (Инкапсуляция, Наследование, Полиморфизм, Абстракция)
- Соблюдай принципы SOLID:
  - Single Responsibility Principle
  - Open/Closed Principle
  - Liskov Substitution Principle
  - Interface Segregation Principle
  - Dependency Inversion Principle
- Применяй KISS (Keep It Simple, Stupid)
- Следуй DRY (Don't Repeat Yourself)
- Используй YAGNI (You Aren't Gonna Need It)

### Качество кода
- Не используй устаревшие практики и библиотеки
- Не используй устаревшие методы
- Пиши краткий, лаконичный и понятный код
- Избегай code smells
- Не оставляй неиспользуемый код
- Удаляй закомментированный код
- Используй meaningful names для переменных и методов

## Документирование

### Процесс разработки
1. **Перед каждым значительным шагом**:
   - Спрашивай подтверждения
   - Объясняй, что будет сделано
   - Предлагай альтернативы при неоднозначности

2. **После каждого шага**:
   - Предоставляй краткое резюме (не более 5 пунктов)
   - Обновляй документацию
   - Отмечай выполненные задачи в Tasktracker.md

3. **При возникновении проблем**:
   - Предлагай 2-3 альтернативных подхода
   - Объясняй преимущества и недостатки каждого
   - Записывай в Diary.md

### Обязательные документы
Всегда актуализируй следующие файлы:

#### /docs/Project.md
- Обновляй при изменении архитектуры
- Добавляй новые компоненты и их описание
- Обновляй диаграммы (Mermaid)
- Документируй API изменения

#### /docs/Tasktracker.md
- Отмечай статус задач
- Добавляй новые задачи с приоритетом
- Указывай зависимости между задачами
- Обновляй прогресс

#### /docs/Diary.md
Формат записи:
```markdown
## 📅 YYYY-MM-DD

### 🔍 Наблюдения
- Что было замечено/обнаружено

### 💡 Решения
- Принятые решения с обоснованием
- Рассмотренные альтернативы

### ⚠️ Проблемы
- Возникшие проблемы
- Предложенные решения
- Текущий статус

### 📋 Технические долги
- Что нужно будет сделать позже

### 🎯 Следующие шаги
- Планы на следующий период
```

### Документирование кода

#### Заголовок файла
Каждый новый файл должен начинаться с:
```java
/**
 * @file: [имя файла]
 * @description: [краткое описание]
 * @dependencies: [связанные компоненты/файлы]
 * @created: [дата]
 */
```

#### JavaDoc
- Документируй все public методы
- Описывай параметры (@param)
- Описывай возвращаемые значения (@return)
- Описывай исключения (@throws)

Пример:
```java
/**
 * Создает новый лендинг для организации
 *
 * @param request DTO с данными для создания лендинга
 * @return созданный лендинг
 * @throws DuplicateDomainException если домен уже существует
 * @throws OrganizationNotFoundException если организация не найдена
 */
public LandingResponseDto createLanding(LandingRequestDto request) {
    // implementation
}
```

## Структура проекта

### Пакетная структура микросервиса
```
com.baganov.klassifikator.{service-name}
├── config/              # Конфигурация Spring
├── controller/          # REST контроллеры
├── service/             # Бизнес-логика
│   └── impl/           # Реализации сервисов
├── repository/          # Data Access Layer (Spring Data JPA)
├── model/              # Domain модели
│   ├── entity/         # JPA entities
│   └── dto/            # Data Transfer Objects
├── mapper/             # Маппинг между entity и dto (MapStruct)
├── exception/          # Кастомные исключения
├── util/               # Утилиты
└── {ServiceName}Application.java
```

### Naming Conventions

#### Java классы
- Controllers: `{Entity}Controller` (например, `LandingController`)
- Services: `{Entity}Service` (интерфейс), `{Entity}ServiceImpl` (реализация)
- Repositories: `{Entity}Repository`
- Entities: `{Entity}` (например, `Landing`, `Organization`)
- DTOs: `{Entity}Dto`, `{Entity}RequestDto`, `{Entity}ResponseDto`
- Exceptions: `{Description}Exception` (например, `DuplicateDomainException`)
- Mappers: `{Entity}Mapper`

#### REST API
- Используй множественное число: `/landings`, `/organizations`
- Версионирование: `/api/v1/...`
- HTTP методы:
  - GET: получение данных
  - POST: создание
  - PUT: полное обновление
  - PATCH: частичное обновление
  - DELETE: удаление

#### База данных
- Таблицы: snake_case, множественное число (например, `organizations`, `landing_pages`)
- Колонки: snake_case (например, `created_at`, `organization_id`)
- Индексы: `idx_{table}_{column}` (например, `idx_landings_domain`)
- Foreign keys: `fk_{table}_{referenced_table}` (например, `fk_landings_organizations`)

## Код-стайл

### Java
- Используй Google Java Style Guide
- Checkstyle должен быть включен
- Используй Lombok для уменьшения boilerplate:
  - `@Data`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor`
  - `@Slf4j` для логирования
- Максимальная длина строки: 120 символов
- Используй Optional вместо null где возможно

### SQL
- Ключевые слова UPPERCASE: `SELECT`, `FROM`, `WHERE`
- Названия таблиц и колонок lowercase
- Используй Flyway для миграций
- Формат миграций: `V{version}__{description}.sql`
  - Пример: `V001__create_organizations_table.sql`

### JavaScript
- Используй ES6+ синтаксис
- Используй `const` и `let`, избегай `var`
- Используй arrow functions где уместно
- Используй template literals вместо конкатенации строк

## Тестирование

### Обязательные требования
- Минимум 80% code coverage
- 100% для критичной бизнес-логики
- Каждый public метод должен иметь тест

### Типы тестов

#### Unit тесты
- Используй JUnit 5
- Используй Mockito для моков
- Используй AssertJ для assertions
- Naming: `{methodName}_Should{ExpectedBehavior}_When{Condition}`

Пример:
```java
@Test
void createLanding_ShouldThrowException_WhenDomainAlreadyExists() {
    // given
    // when
    // then
}
```

#### Integration тесты
- Используй Spring Boot Test
- Используй Testcontainers для БД
- Тестируй реальные взаимодействия с БД
- Naming: `{scenario}IntegrationTest`

#### E2E тесты
- Используй REST Assured
- Тестируй полные сценарии использования
- Naming: `{scenario}E2ETest`

### Что тестировать
- ✅ Бизнес-логику в сервисах
- ✅ Валидацию данных
- ✅ Обработку исключений
- ✅ Маппинг между entity и dto
- ✅ REST endpoints
- ✅ Запросы к БД
- ❌ Геттеры/сеттеры
- ❌ Конструкторы без логики
- ❌ Конфигурационные классы

## Обработка ошибок

### Кастомные исключения
Создавай специфичные исключения для бизнес-логики:
```java
public class DuplicateDomainException extends RuntimeException {
    public DuplicateDomainException(String domain) {
        super(String.format("Domain %s already exists", domain));
    }
}
```

### Global Exception Handler
Используй `@ControllerAdvice` для централизованной обработки:
```java
@ControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(DuplicateDomainException.class)
    public ResponseEntity<ErrorResponse> handleDuplicateDomain(DuplicateDomainException ex) {
        // handle exception
    }
}
```

### Формат ошибок API
```json
{
  "timestamp": "2025-11-02T10:00:00Z",
  "status": 400,
  "error": "Bad Request",
  "message": "Domain modernissimo.volzhck.ru already exists",
  "path": "/api/v1/landings"
}
```

## Логирование

### Уровни логирования
- `ERROR`: Критичные ошибки, требующие немедленного внимания
- `WARN`: Предупреждения, потенциальные проблемы
- `INFO`: Важные события (создание/удаление сущностей)
- `DEBUG`: Детальная информация для отладки
- `TRACE`: Очень детальная информация

### Что логировать
- ✅ Входящие запросы (INFO)
- ✅ Исключения (ERROR с stack trace)
- ✅ Важные бизнес-события (INFO)
- ✅ Медленные операции (WARN)
- ✅ Внешние API вызовы (INFO/DEBUG)
- ❌ Чувствительные данные (пароли, токены)
- ❌ Персональные данные в production

### Формат логов
```java
@Slf4j
public class LandingServiceImpl implements LandingService {
    
    public LandingResponseDto createLanding(LandingRequestDto request) {
        log.info("Creating landing for organization {} with domain {}", 
                 request.getOrganizationId(), request.getDomain());
        try {
            // logic
            log.info("Successfully created landing with id {}", landing.getId());
            return response;
        } catch (Exception e) {
            log.error("Failed to create landing for organization {}", 
                     request.getOrganizationId(), e);
            throw e;
        }
    }
}
```

## Git Workflow

### Branching Strategy
- `master` - production код (защищенная ветка)
- `develop` - development ветка
- `feature/{feature-name}` - новые фичи
- `bugfix/{bug-name}` - исправления багов
- `hotfix/{issue}` - критичные исправления в production

### Commit Messages
Формат:
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Types**:
- `feat`: новая функциональность
- `fix`: исправление бага
- `docs`: изменения в документации
- `style`: форматирование кода (без изменения логики)
- `refactor`: рефакторинг кода
- `test`: добавление/изменение тестов
- `chore`: изменения в build процессе, зависимостях

**Пример**:
```
feat(landing-service): add landing creation endpoint

Implement POST /api/v1/landings endpoint with:
- Domain uniqueness validation
- Organization existence check
- SSL certificate generation

Closes #123
```

### Code Review Checklist
Перед созданием Pull Request проверь:
- [ ] Код соответствует требованиям задачи
- [ ] Соблюдены SOLID принципы
- [ ] Код покрыт тестами (coverage > 80%)
- [ ] Все тесты проходят
- [ ] Нет code smells (SonarQube)
- [ ] Документация обновлена
- [ ] Нет hardcoded значений
- [ ] Обработка ошибок реализована
- [ ] Логирование добавлено
- [ ] Нет неиспользуемого кода
- [ ] Нет закомментированного кода
- [ ] Commit messages осмысленные

## Безопасность

### Обязательные практики
- ✅ Всегда используй PreparedStatement (защита от SQL injection)
- ✅ Валидируй и санитизируй входные данные (защита от XSS)
- ✅ Используй HTTPS для всех соединений
- ✅ Храни пароли в зашифрованном виде (BCrypt)
- ✅ Используй JWT для аутентификации
- ✅ Настрой CORS правильно
- ✅ Используй rate limiting для API
- ✅ Логируй попытки несанкционированного доступа
- ❌ Никогда не коммить credentials в git
- ❌ Никогда не логировать чувствительные данные
- ❌ Никогда не возвращать stack traces в API responses (production)

### Конфигурация
Используй переменные окружения для чувствительных данных:
```yaml
# application.yml
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
```

## Performance

### Базы данных
- Используй индексы для часто запрашиваемых колонок
- Избегай N+1 проблем (используй fetch join)
- Используй пагинацию для больших списков
- Используй batch операции для массовых вставок/обновлений
- Настрой connection pool правильно

### Кэширование
- Кэшируй часто запрашиваемые данные (Redis)
- Используй Spring Cache annotations
- Определи правильный TTL для разных типов данных
- Реализуй стратегию инвалидации кэша

### API
- Используй пагинацию для списков
- Используй projection для ограничения возвращаемых полей
- Используй HTTP caching headers
- Используй compression (gzip)

## Мониторинг

### Метрики
Отслеживай:
- Response time (p50, p95, p99)
- Request rate (RPS)
- Error rate
- CPU/Memory usage
- Database connections
- Cache hit rate

### Health Checks
Реализуй health check endpoints:
```java
@RestController
@RequestMapping("/actuator")
public class HealthController {
    
    @GetMapping("/health")
    public ResponseEntity<HealthStatus> health() {
        // check database, redis, s3 connectivity
        return ResponseEntity.ok(healthStatus);
    }
}
```

## Перед началом работы

### Чеклист
1. [ ] Прочитай /docs/Project.md
2. [ ] Ознакомься с /docs/Tasktracker.md
3. [ ] Проверь текущие задачи в Tasktracker.md
4. [ ] Прочитай последние записи в /docs/Diary.md
5. [ ] Убедись, что понимаешь архитектуру системы
6. [ ] Убедись, что понимаешь текущую задачу
7. [ ] Спроси подтверждения перед началом

### После завершения работы

1. [ ] Обнови /docs/Tasktracker.md (отметь выполненные задачи)
2. [ ] Добавь запись в /docs/Diary.md (если были важные решения/проблемы)
3. [ ] Обнови /docs/Project.md (если изменилась архитектура)
4. [ ] Запусти тесты
5. [ ] Проверь code coverage
6. [ ] Проверь линтеры
7. [ ] Создай осмысленный commit
8. [ ] Предоставь краткое резюме изменений (не более 5 пунктов)

## Важно помнить

- 🎯 Качество важнее скорости
- 📚 Документация так же важна, как код
- 🧪 Тестирование обязательно
- 🔒 Безопасность превыше всего
- 📊 Мониторинг критичен для production
- 🤝 Коммуникация с командой важна
- 🔄 Рефакторинг - это нормально
- 💡 Всегда есть место для улучшений

---

**Версия**: 1.0  
**Дата создания**: 2025-11-02  
**Последнее обновление**: 2025-11-02

