apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-service
  namespace: klassifikator
  labels:
    app: media-service
    component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: media-service
  template:
    metadata:
      labels:
        app: media-service
        component: microservice
    spec:
      imagePullSecrets:
      - name: timeweb-registry
      containers:
      - name: media-service
        image: 12df3fb9-wise-cepheus.registry.twcstorage.ru/media-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8084
          name: http
        envFrom:
        - configMapRef:
            name: klassifikator-config
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres-service:5432/klassifikator"
        - name: SPRING_DATASOURCE_USERNAME
          value: "klassifikator"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: klassifikator-secrets
              key: DB_PASSWORD
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: klassifikator-secrets
              key: MINIO_ROOT_PASSWORD
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8084
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: klassifikator
  labels:
    app: media-service
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
    name: http
  selector:
    app: media-service

