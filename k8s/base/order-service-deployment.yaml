apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: klassifikator
  labels:
    app: order-service
    component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        component: microservice
    spec:
      imagePullSecrets:
      - name: timeweb-registry
      containers:
      - name: order-service
        image: 12df3fb9-wise-cepheus.registry.twcstorage.ru/order-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8086
          name: http
        envFrom:
        - configMapRef:
            name: klassifikator-config
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres-service:5432/klassifikator"
        - name: SPRING_DATASOURCE_USERNAME
          value: "klassifikator"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: klassifikator-secrets
              key: DB_PASSWORD
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8086
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8086
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: klassifikator
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
  - port: 8086
    targetPort: 8086
    protocol: TCP
    name: http
  selector:
    app: order-service

