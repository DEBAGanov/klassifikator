# Google Sheets Integration - Руководство

**Версия**: 1.0  
**Дата создания**: 2025-11-03

---

## 📋 Описание

Интеграция с Google Sheets позволяет автоматически синхронизировать данные организаций, товаров и акций из Google Таблиц в систему Klassifikator.

---

## 🎯 Возможности

- ✅ Автоматический парсинг данных из Google Sheets
- ✅ Синхронизация контента организаций
- ✅ Синхронизация товаров
- ✅ Синхронизация акций
- ✅ Загрузка изображений из URL в S3
- ✅ Автоматическая синхронизация по расписанию
- ✅ Ручная синхронизация через API
- ✅ Отслеживание статуса синхронизации

---

## 🔧 Настройка Google Sheets API

### Шаг 1: Создание проекта в Google Cloud

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Название проекта: `Klassifikator`

### Шаг 2: Включение Google Sheets API

1. В меню выберите **APIs & Services** → **Library**
2. Найдите **Google Sheets API**
3. Нажмите **Enable**

### Шаг 3: Создание Service Account

1. Перейдите в **APIs & Services** → **Credentials**
2. Нажмите **Create Credentials** → **Service Account**
3. Заполните данные:
   - Service account name: `klassifikator-sheets`
   - Service account ID: `klassifikator-sheets`
   - Description: `Service account for Klassifikator Google Sheets integration`
4. Нажмите **Create and Continue**
5. Grant this service account access to project: выберите роль **Editor**
6. Нажмите **Done**

### Шаг 4: Создание ключа

1. Найдите созданный Service Account в списке
2. Нажмите на него
3. Перейдите на вкладку **Keys**
4. Нажмите **Add Key** → **Create new key**
5. Выберите тип **JSON**
6. Нажмите **Create**
7. Файл `credentials.json` будет скачан

### Шаг 5: Настройка credentials

```bash
# Скопируйте файл credentials.json в корень проекта
cp ~/Downloads/credentials.json /path/to/klassifikator/credentials.json

# Или установите переменную окружения
export GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/credentials.json
```

### Шаг 6: Предоставление доступа к таблице

1. Откройте ваш файл `credentials.json`
2. Найдите поле `client_email` (например, `klassifikator-sheets@project-id.iam.gserviceaccount.com`)
3. Откройте вашу Google Таблицу
4. Нажмите **Share**
5. Добавьте email из `client_email` с правами **Editor**
6. Нажмите **Send**

---

## 📊 Формат Google Sheets

### Структура таблицы

Таблица должна содержать следующие колонки:

#### Основные данные организации (первая строка)

| Название | Категория | Тип | Адрес | Телефон | Email | Режим работы | Title | Description | H1 | О нас |
|----------|-----------|-----|-------|---------|-------|--------------|-------|-------------|----|----|
| Модернисимо | Мебель | Компания | г. Волжский, ул. Мира, 10 | +79001234567 | info@modernissimo.ru | Пн-Пт: 9:00-18:00 | Модернисимо - Современная мебель | Качественная мебель для дома | Модернисимо | Мы предлагаем... |

#### Товары (следующие строки)

| Товар | Описание товара | Цена | Категория | Изображение |
|-------|----------------|------|-----------|-------------|
| Стол офисный | Удобный офисный стол | 15000 | Столы | https://example.com/table.jpg |
| Кресло офисное | Эргономичное кресло | 12000 | Кресла | https://example.com/chair.jpg |

#### Акции (дополнительные строки)

| Акция | Описание акции | Действует до |
|-------|---------------|--------------|
| Скидка 20% | При покупке комплекта | 31.12.2025 |
| Бесплатная доставка | При заказе от 50000 руб | 31.01.2026 |

### Пример таблицы

Смотрите [пример таблицы](https://docs.google.com/spreadsheets/d/1KS2TOS5ZKxONDmUaVoiwb3tyu3Y1DlQ/edit?gid=0#gid=0)

---

## 🔌 API Endpoints

### 1. Создание синхронизации

```bash
POST /api/v1/integration/sheets/sync
Content-Type: application/json

{
  "organizationId": 1,
  "spreadsheetId": "1KS2TOS5ZKxONDmUaVoiwb3tyu3Y1DlQ",
  "sheetName": "Лист1",
  "syncIntervalMinutes": 30,
  "isActive": true
}
```

**Ответ:**
```json
{
  "id": 1,
  "organizationId": 1,
  "spreadsheetId": "1KS2TOS5ZKxONDmUaVoiwb3tyu3Y1DlQ",
  "sheetName": "Лист1",
  "syncIntervalMinutes": 30,
  "isActive": true,
  "lastSyncAt": null,
  "lastSyncStatus": null,
  "createdAt": "2025-11-03T22:00:00"
}
```

### 2. Получение настроек синхронизации

```bash
GET /api/v1/integration/sheets/sync/organization/{organizationId}
```

**Ответ:**
```json
{
  "id": 1,
  "organizationId": 1,
  "spreadsheetId": "1KS2TOS5ZKxONDmUaVoiwb3tyu3Y1DlQ",
  "sheetName": "Лист1",
  "syncIntervalMinutes": 30,
  "isActive": true,
  "lastSyncAt": "2025-11-03T22:30:00",
  "lastSyncStatus": "SUCCESS - Processed 15 rows",
  "createdAt": "2025-11-03T22:00:00"
}
```

### 3. Ручная синхронизация

```bash
POST /api/v1/integration/sheets/sync/{organizationId}/trigger
```

**Ответ:**
```json
{
  "message": "Sync triggered successfully",
  "organizationId": 1,
  "status": "SUCCESS"
}
```

### 4. Чтение данных из таблицы

```bash
GET /api/v1/integration/sheets/read?spreadsheetId=1KS2TOS5ZKxONDmUaVoiwb3tyu3Y1DlQ&range=A:Z
```

**Ответ:**
```json
[
  {
    "Название": "Модернисимо",
    "Телефон": "+79001234567",
    "Email": "info@modernissimo.ru",
    ...
  },
  {
    "Товар": "Стол офисный",
    "Цена": "15000",
    ...
  }
]
```

---

## ⚙️ Конфигурация

### application.yml

```yaml
# Google Sheets Configuration
google:
  sheets:
    credentials-path: ${GOOGLE_SHEETS_CREDENTIALS_PATH:./credentials.json}
    application-name: ${GOOGLE_SHEETS_APP_NAME:Klassifikator}
    sync-interval-ms: ${GOOGLE_SHEETS_SYNC_INTERVAL:1800000} # 30 minutes

# Other Services URLs
content-service:
  url: ${CONTENT_SERVICE_URL:http://localhost:8082}

media-service:
  url: ${MEDIA_SERVICE_URL:http://localhost:8084}
```

### Переменные окружения

```bash
# Google Sheets
export GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/credentials.json
export GOOGLE_SHEETS_APP_NAME=Klassifikator
export GOOGLE_SHEETS_SYNC_INTERVAL=1800000  # 30 минут

# Другие сервисы
export CONTENT_SERVICE_URL=http://localhost:8082
export MEDIA_SERVICE_URL=http://localhost:8084
```

---

## 🔄 Процесс синхронизации

### Автоматическая синхронизация

Система автоматически синхронизирует данные каждые 30 минут (по умолчанию) для всех активных организаций.

```java
@Scheduled(fixedDelayString = "${google.sheets.sync-interval-ms:1800000}")
public void syncAllActiveOrganizations() {
    // Синхронизация всех активных организаций
}
```

### Ручная синхронизация

```bash
# Синхронизация конкретной организации
curl -X POST http://localhost:8085/api/v1/integration/sheets/sync/1/trigger
```

### Процесс обработки данных

1. **Чтение данных из Google Sheets**
   - Подключение к Google Sheets API
   - Чтение диапазона A:Z
   - Преобразование в Map<String, Object>

2. **Парсинг данных**
   - Первая строка: данные организации
   - Следующие строки: товары (если есть колонка "Товар")
   - Дополнительные строки: акции (если есть колонка "Акция")

3. **Обновление данных**
   - Обновление контента организации через Content Service
   - Создание/обновление товаров
   - Создание/обновление акций

4. **Обработка изображений**
   - Скачивание изображений по URL
   - Загрузка в S3 через Media Service
   - Обновление ссылок на изображения

5. **Сохранение статуса**
   - Обновление `last_sync_at`
   - Обновление `last_sync_status`

---

## 📝 Маппинг колонок

### Контент организации

| Колонка Google Sheets | Поле в БД | Обязательное |
|-----------------------|-----------|--------------|
| Название | organizationName, h1, title | Да |
| Описание | description | Нет |
| О нас | aboutUs | Нет |
| Телефон | phone | Да |
| Email | email | Нет |
| Адрес | address | Нет |
| Режим работы | workingHours | Нет |
| Title | title | Нет |
| Description | description | Нет |
| H1 | h1 | Нет |

### Товары

| Колонка Google Sheets | Поле в БД | Обязательное |
|-----------------------|-----------|--------------|
| Товар | name | Да |
| Описание товара | description | Нет |
| Цена | price | Да |
| Категория | category | Нет |
| Изображение | imageUrl → S3 | Нет |

### Акции

| Колонка Google Sheets | Поле в БД | Обязательное |
|-----------------------|-----------|--------------|
| Акция | title | Да |
| Описание акции | description | Нет |
| Действует до | validTo | Нет |

---

## 🖼️ Загрузка изображений

### Из Google Sheets

Если в колонке "Изображение" указан URL, система автоматически:

1. Скачивает изображение по URL
2. Загружает в S3 (MinIO)
3. Сохраняет метаданные в БД
4. Обновляет ссылку на изображение в товаре

### Поддерживаемые форматы

- JPEG (.jpg, .jpeg)
- PNG (.png)
- GIF (.gif)
- WebP (.webp)

### Пример

```
Колонка "Изображение": https://example.com/images/table.jpg
↓
Скачивание изображения
↓
Загрузка в S3: organizations/1/uuid_table.jpg
↓
URL в БД: http://localhost:9000/klassifikator/organizations/1/uuid_table.jpg
```

---

## 🧪 Тестирование

### 1. Создание тестовой таблицы

1. Создайте новую Google Таблицу
2. Добавьте колонки согласно формату
3. Заполните тестовыми данными
4. Предоставьте доступ Service Account

### 2. Настройка синхронизации

```bash
curl -X POST http://localhost:8085/api/v1/integration/sheets/sync \
  -H "Content-Type: application/json" \
  -d '{
    "organizationId": 1,
    "spreadsheetId": "YOUR_SPREADSHEET_ID",
    "sheetName": "Лист1",
    "syncIntervalMinutes": 30,
    "isActive": true
  }'
```

### 3. Ручная синхронизация

```bash
curl -X POST http://localhost:8085/api/v1/integration/sheets/sync/1/trigger
```

### 4. Проверка результатов

```bash
# Проверка контента
curl http://localhost:8082/api/v1/content/organization/1

# Проверка товаров
curl http://localhost:8082/api/v1/products/organization/1

# Проверка акций
curl http://localhost:8082/api/v1/promotions/organization/1

# Проверка статуса синхронизации
curl http://localhost:8085/api/v1/integration/sheets/sync/organization/1
```

---

## 🐛 Troubleshooting

### Проблема: "Failed to read spreadsheet"

**Причины:**
- Неверный Spreadsheet ID
- Service Account не имеет доступа к таблице
- Неверный путь к credentials.json

**Решение:**
1. Проверьте Spreadsheet ID в URL таблицы
2. Убедитесь, что Service Account добавлен в список редакторов таблицы
3. Проверьте путь к credentials.json

### Проблема: "Sync failed: Unauthorized"

**Причины:**
- Неверные credentials
- API не включен

**Решение:**
1. Проверьте файл credentials.json
2. Убедитесь, что Google Sheets API включен в проекте
3. Перезапустите Integration Service

### Проблема: "Failed to update content"

**Причины:**
- Content Service недоступен
- Неверный формат данных

**Решение:**
1. Проверьте доступность Content Service: `curl http://localhost:8082/actuator/health`
2. Проверьте логи Integration Service
3. Проверьте формат данных в таблице

### Проблема: "Failed to upload image"

**Причины:**
- Неверный URL изображения
- Media Service недоступен
- S3 недоступен

**Решение:**
1. Проверьте URL изображения (должен быть публично доступен)
2. Проверьте Media Service: `curl http://localhost:8084/actuator/health`
3. Проверьте MinIO: `curl http://localhost:9000/minio/health/live`

---

## 📊 Мониторинг

### Проверка статуса синхронизации

```bash
# Получить статус синхронизации
curl http://localhost:8085/api/v1/integration/sheets/sync/organization/1

# Ответ
{
  "lastSyncAt": "2025-11-03T22:30:00",
  "lastSyncStatus": "SUCCESS - Processed 15 rows"
}
```

### Логи

```bash
# Логи Integration Service
tail -f logs/Integration\ Service.log

# Поиск ошибок синхронизации
grep "Failed to sync" logs/Integration\ Service.log

# Поиск успешных синхронизаций
grep "Successfully synced" logs/Integration\ Service.log
```

### Метрики

- Количество синхронизаций: `syncAllActiveOrganizations` executions
- Время синхронизации: duration of `syncOrganizationData`
- Ошибки синхронизации: exceptions in sync methods

---

## 🔒 Безопасность

### Рекомендации

1. **Храните credentials.json в безопасном месте**
   - Не коммитьте в Git
   - Используйте .gitignore
   - Ограничьте права доступа к файлу

2. **Ограничьте права Service Account**
   - Минимальные необходимые права
   - Только для нужных таблиц

3. **Используйте HTTPS**
   - Для загрузки изображений
   - Для API запросов

4. **Валидация данных**
   - Проверка формата данных
   - Санитизация входных данных
   - Ограничение размера изображений

---

## 📚 Дополнительные ресурсы

- [Google Sheets API Documentation](https://developers.google.com/sheets/api)
- [Service Account Documentation](https://cloud.google.com/iam/docs/service-accounts)
- [Google Cloud Console](https://console.cloud.google.com/)

---

**Автор**: AI Assistant  
**Дата последнего обновления**: 2025-11-03

