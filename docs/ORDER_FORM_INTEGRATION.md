# Интеграция формы заказа на лендинге

**Версия**: 1.0  
**Дата создания**: 2025-11-03

---

## 📋 Описание

Документация по интеграции формы заказа на лендингах. Форма позволяет пользователям добавлять товары в корзину и оформлять заказы прямо с лендинга.

---

## 🎯 Возможности

### Основной функционал
- ✅ Добавление товаров в корзину
- ✅ Изменение количества товаров
- ✅ Удаление товаров из корзины
- ✅ Автоматический расчет общей суммы
- ✅ Валидация данных формы
- ✅ Форматирование телефона при вводе
- ✅ Отправка заказа через API
- ✅ Уведомления об успехе/ошибках
- ✅ Адаптивный дизайн (mobile-first)
- ✅ Интеграция с Google Analytics

---

## 📦 Файлы

### 1. `order-form.js` (JavaScript)
Основная логика работы с корзиной и отправкой заказов.

**Функции**:
- `addToCart(productId, productName, price)` - добавить товар в корзину
- `removeFromCart(productId)` - удалить товар из корзины
- `updateQuantity(productId, quantity)` - изменить количество
- `updateCartUI()` - обновить отображение корзины
- `openOrderModal()` - открыть модальное окно заказа
- `closeOrderModal()` - закрыть модальное окно
- `submitOrder(event)` - отправить заказ
- `showNotification(message, type)` - показать уведомление
- `formatPrice(price)` - форматировать цену
- `formatPhoneInput(input)` - форматировать телефон
- `validatePhone(input)` - валидировать телефон

### 2. `order-form.css` (Стили)
Стили для формы заказа, корзины и модального окна.

**Компоненты**:
- Модальное окно заказа
- Корзина товаров
- Форма заказа
- Кнопки и элементы управления
- Уведомления
- Адаптивные стили для мобильных устройств

### 3. `index-with-order.html` (HTML шаблон)
Полный шаблон лендинга с интегрированной формой заказа.

**Секции**:
- Header с кнопкой корзины
- Hero section
- О нас
- Акции
- Каталог товаров (с кнопками "Добавить в корзину")
- Контакты
- Footer
- Модальное окно заказа

---

## 🔧 Установка

### Шаг 1: Подключение файлов

Добавьте в `<head>` вашего HTML:

```html
<meta name="organization-id" content="{{organizationId}}">
<meta name="landing-id" content="{{landingId}}">
<link rel="stylesheet" href="order-form.css">
```

Добавьте перед закрывающим `</body>`:

```html
<script src="order-form.js"></script>
```

### Шаг 2: Добавление кнопки корзины

В header добавьте:

```html
<button class="btn btn-primary" onclick="openOrderModal()">
    <span class="cart-badge">
        🛒 Корзина
        <span id="cart-count" class="cart-count">0</span>
    </span>
</button>
```

### Шаг 3: Добавление кнопок "Добавить в корзину"

На карточках товаров:

```html
<button class="add-to-cart-btn" 
        onclick="addToCart({{id}}, '{{name}}', {{price}})">
    Добавить в корзину
</button>
```

### Шаг 4: Добавление модального окна

Скопируйте модальное окно из `index-with-order.html`:

```html
<div id="order-modal" class="order-modal">
    <!-- Содержимое модального окна -->
</div>
```

---

## 💻 Использование

### Добавление товара в корзину

```javascript
// Вызов функции при клике на кнопку
addToCart(productId, productName, price);

// Пример
addToCart(1, 'Стол офисный', 15000);
```

### Открытие формы заказа

```javascript
// Программно
openOrderModal();

// Или через кнопку
<button onclick="openOrderModal()">Оформить заказ</button>
```

### Закрытие формы

```javascript
// Программно
closeOrderModal();

// Автоматически закрывается:
// - По клику на крестик
// - По клику вне модального окна
// - По нажатию ESC
// - После успешного оформления заказа
```

---

## 📝 Формат данных заказа

### Отправляемые данные

```javascript
{
  "organizationId": 1,
  "landingId": 1,
  "customerName": "Иван Иванов",
  "customerPhone": "+79001234567",
  "customerEmail": "ivan@example.com",
  "deliveryAddress": "г. Москва, ул. Ленина, д. 1",
  "comment": "Доставка с 10:00 до 18:00",
  "items": [
    {
      "productId": 1,
      "quantity": 2
    },
    {
      "productId": 2,
      "quantity": 1
    }
  ]
}
```

### API Endpoint

```
POST /api/v1/orders
Content-Type: application/json
```

---

## ✅ Валидация

### Обязательные поля
- `customerName` - имя клиента (не пустое)
- `customerPhone` - телефон клиента (не пустое, валидный формат)
- `items` - список товаров (не пустой)

### Опциональные поля
- `customerEmail` - email клиента (валидация формата email)
- `deliveryAddress` - адрес доставки
- `comment` - комментарий к заказу

### Валидация на клиенте

```javascript
// Автоматическая валидация HTML5
<input type="text" required>
<input type="email">
<input type="tel" required>

// Кастомная валидация телефона
function validatePhone(input) {
    const phone = input.value.replace(/\D/g, '');
    if (phone.length > 0 && phone.length < 10) {
        input.setCustomValidity('Введите корректный номер телефона');
    } else {
        input.setCustomValidity('');
    }
}
```

---

## 🎨 Кастомизация

### Изменение цветов

В `order-form.css`:

```css
/* Основной цвет кнопок */
.btn-primary {
    background: #007bff; /* Измените на ваш цвет */
}

/* Цвет кнопки "Добавить в корзину" */
.add-to-cart-btn {
    background: #28a745; /* Измените на ваш цвет */
}

/* Цвет уведомлений об успехе */
.notification-success {
    background: #d4edda;
    border-left: 4px solid #28a745;
}
```

### Изменение текстов

В `order-form.js`:

```javascript
// Текст уведомления при добавлении в корзину
showNotification('Товар добавлен в корзину', 'success');

// Текст уведомления об успешном заказе
showNotification('Заказ успешно оформлен! Мы свяжемся с вами в ближайшее время.', 'success');
```

### Изменение формата телефона

В `order-form.js`:

```javascript
function formatPhoneInput(input) {
    let phone = input.value.replace(/\D/g, '');
    
    // Измените формат по вашему усмотрению
    if (phone.length > 0) {
        if (phone[0] === '8') {
            phone = '7' + phone.slice(1);
        }
        
        let formatted = '+7';
        if (phone.length > 1) {
            formatted += ' (' + phone.slice(1, 4);
        }
        // ... остальная логика
    }
}
```

---

## 📊 Интеграция с аналитикой

### Google Analytics 4

Автоматически отправляется событие `purchase` при успешном оформлении заказа:

```javascript
if (typeof gtag !== 'undefined') {
    gtag('event', 'purchase', {
        transaction_id: order.id,
        value: order.totalAmount,
        currency: 'RUB',
        items: cart.map(item => ({
            item_id: item.productId,
            item_name: item.productName,
            price: item.price,
            quantity: item.quantity
        }))
    });
}
```

### Добавление других событий

```javascript
// При добавлении товара в корзину
function addToCart(productId, productName, price) {
    // ... существующий код ...
    
    // Отправка события в GA
    if (typeof gtag !== 'undefined') {
        gtag('event', 'add_to_cart', {
            items: [{
                item_id: productId,
                item_name: productName,
                price: price,
                quantity: 1
            }]
        });
    }
}

// При открытии формы заказа
function openOrderModal() {
    // ... существующий код ...
    
    if (typeof gtag !== 'undefined') {
        gtag('event', 'begin_checkout', {
            items: cart.map(item => ({
                item_id: item.productId,
                item_name: item.productName,
                price: item.price,
                quantity: item.quantity
            }))
        });
    }
}
```

---

## 🔒 Безопасность

### Защита от XSS

```javascript
// Все данные экранируются при вставке в DOM
const sanitize = (str) => {
    const div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
};

// Использование
element.textContent = userInput; // Безопасно
element.innerHTML = sanitize(userInput); // Безопасно
```

### CSRF защита

```javascript
// Добавление CSRF токена (если требуется)
const response = await fetch(`${API_BASE_URL}/api/v1/orders`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'X-CSRF-Token': getCsrfToken() // Ваша функция получения токена
    },
    body: JSON.stringify(orderData)
});
```

---

## 📱 Адаптивность

### Брейкпоинты

```css
/* Планшеты */
@media (max-width: 768px) {
    .cart-item {
        grid-template-columns: 1fr;
    }
}

/* Мобильные устройства */
@media (max-width: 480px) {
    .order-modal-content {
        border-radius: 0;
    }
}
```

### Тестирование на устройствах

- ✅ iPhone (Safari)
- ✅ Android (Chrome)
- ✅ iPad (Safari)
- ✅ Desktop (Chrome, Firefox, Safari, Edge)

---

## 🐛 Отладка

### Включение debug режима

В `order-form.js` добавьте:

```javascript
const DEBUG = true;

function debugLog(...args) {
    if (DEBUG) {
        console.log('[OrderForm]', ...args);
    }
}

// Использование
debugLog('Cart updated:', cart);
debugLog('Order submitted:', orderData);
```

### Проверка отправки заказа

```javascript
// В консоли браузера
console.log('Cart:', cart);
console.log('Organization ID:', ORGANIZATION_ID);
console.log('Landing ID:', LANDING_ID);
```

---

## ⚠️ Известные ограничения

1. **LocalStorage не используется**
   - Корзина очищается при перезагрузке страницы
   - Решение: добавить сохранение в localStorage

2. **Нет проверки наличия товара**
   - Можно добавить неактивный товар
   - Решение: проверка на сервере при создании заказа

3. **Нет минимальной суммы заказа**
   - Можно оформить заказ на любую сумму
   - Решение: добавить проверку минимальной суммы

---

## 🚀 Будущие улучшения

1. **Сохранение корзины**
   - Использование localStorage
   - Восстановление корзины при возврате

2. **Промокоды и скидки**
   - Поле для ввода промокода
   - Автоматический расчет скидки

3. **Выбор способа доставки**
   - Самовывоз
   - Курьерская доставка
   - Почта России

4. **Выбор способа оплаты**
   - Наличными
   - Картой онлайн
   - При получении

5. **История заказов**
   - Просмотр предыдущих заказов
   - Повторный заказ

---

## 📚 Примеры использования

### Пример 1: Простой лендинг с 1 товаром

```html
<button class="btn btn-primary" 
        onclick="addToCart(1, 'Консультация', 5000); openOrderModal();">
    Заказать консультацию - 5000 руб.
</button>
```

### Пример 2: Каталог товаров

```html
{{#each products}}
<div class="product-card">
    <h3>{{name}}</h3>
    <p>{{description}}</p>
    <p class="price">{{formatPrice price}} руб.</p>
    <button onclick="addToCart({{id}}, '{{name}}', {{price}})">
        В корзину
    </button>
</div>
{{/each}}
```

### Пример 3: Быстрый заказ

```html
<button onclick="
    addToCart(1, 'Услуга', 10000);
    document.getElementById('customerName').value = 'Быстрый заказ';
    openOrderModal();
">
    Быстрый заказ
</button>
```

---

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что все файлы подключены корректно
3. Проверьте, что meta-теги с ID организации и лендинга установлены
4. Проверьте доступность API endpoint `/api/v1/orders`

---

**Автор**: AI Assistant  
**Дата последнего обновления**: 2025-11-03

