# Руководство по тестированию системы Klassifikator

**Версия**: 1.0  
**Дата создания**: 2025-11-03

---

## 📋 Содержание

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Запуск системы](#запуск-системы)
3. [Тестирование микросервисов](#тестирование-микросервисов)
4. [Тестирование Order Service](#тестирование-order-service)
5. [Тестирование формы заказа](#тестирование-формы-заказа)
6. [Тестирование на мобильных устройствах](#тестирование-на-мобильных-устройствах)
7. [Проверка уведомлений Telegram](#проверка-уведомлений-telegram)
8. [Troubleshooting](#troubleshooting)

---

## 🚀 Подготовка к тестированию

### Требования

#### Системные требования
- **OS**: macOS, Linux, Windows (с WSL2)
- **Java**: JDK 17 или выше
- **Docker**: версия 20.10+
- **Docker Compose**: версия 2.0+
- **RAM**: минимум 8GB (рекомендуется 16GB)
- **Disk**: минимум 10GB свободного места

#### Установленное ПО
```bash
# Проверка Java
java -version

# Проверка Docker
docker --version
docker compose version

# Проверка портов (должны быть свободны)
lsof -i :8080  # API Gateway
lsof -i :8081  # Landing Service
lsof -i :8082  # Content Service
lsof -i :8083  # Template Service
lsof -i :8084  # Media Service
lsof -i :8085  # Integration Service
lsof -i :8086  # Order Service
lsof -i :5432  # PostgreSQL
lsof -i :6379  # Redis
lsof -i :9000  # MinIO
```

### Подготовка окружения

#### 1. Клонирование репозитория
```bash
cd /Users/bagano/Downloads/Cursor/klassifikator
```

#### 2. Проверка конфигурации
```bash
# Проверка наличия docker-compose.yml
ls -la docker-compose.yml

# Проверка наличия скриптов
ls -la *.sh
```

#### 3. Сборка проекта
```bash
./gradlew clean build -x test
```

---

## 🚀 Запуск системы

### Автоматический запуск (рекомендуется)

```bash
# Запуск всех сервисов
./start-services.sh
```

Скрипт выполнит:
1. ✅ Проверку Java и Docker
2. ✅ Запуск Docker Compose (PostgreSQL, Redis, MinIO)
3. ✅ Ожидание готовности БД
4. ✅ Проверку свободных портов
5. ✅ Сборку проекта
6. ✅ Запуск всех микросервисов
7. ✅ Проверку health endpoints

### Ручной запуск

#### 1. Запуск инфраструктуры
```bash
docker compose up -d
```

#### 2. Проверка готовности PostgreSQL
```bash
docker exec klassifikator-postgres pg_isready -U klassifikator_user
```

#### 3. Проверка готовности Redis
```bash
docker exec klassifikator-redis redis-cli ping
```

#### 4. Запуск микросервисов

```bash
# Landing Service
java -jar landing-service/build/libs/landing-service.jar &

# Content Service
java -jar content-service/build/libs/content-service.jar &

# Template Service
java -jar template-service/build/libs/template-service.jar &

# Media Service
java -jar media-service/build/libs/media-service.jar &

# Integration Service
java -jar integration-service/build/libs/integration-service.jar &

# Order Service
java -jar order-service/build/libs/order-service.jar &

# API Gateway
java -jar api-gateway/build/libs/api-gateway.jar &
```

### Проверка запуска

```bash
# Проверка всех health endpoints
curl http://localhost:8081/actuator/health  # Landing Service
curl http://localhost:8082/actuator/health  # Content Service
curl http://localhost:8083/actuator/health  # Template Service
curl http://localhost:8084/actuator/health  # Media Service
curl http://localhost:8085/actuator/health  # Integration Service
curl http://localhost:8086/actuator/health  # Order Service
curl http://localhost:8080/actuator/health  # API Gateway
```

Ожидаемый ответ:
```json
{
  "status": "UP"
}
```

---

## 🧪 Тестирование микросервисов

### Landing Service (8081)

#### 1. Создание организации
```bash
curl -X POST http://localhost:8081/api/v1/organizations \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовая Организация",
    "description": "Описание тестовой организации",
    "category": "Услуги",
    "type": "Компания",
    "address": "г. Москва, ул. Тестовая, д. 1",
    "phone": "+79001234567",
    "email": "test@example.com",
    "website": "https://test.example.com",
    "workingHours": "Пн-Пт: 9:00-18:00",
    "isActive": true
  }'
```

#### 2. Получение организации
```bash
curl http://localhost:8081/api/v1/organizations/1
```

#### 3. Создание лендинга
```bash
curl -X POST http://localhost:8081/api/v1/landings \
  -H "Content-Type: application/json" \
  -d '{
    "organizationId": 1,
    "domain": "test.volzhck.ru",
    "templateId": 1,
    "title": "Тестовый лендинг",
    "description": "Описание тестового лендинга",
    "isActive": true
  }'
```

### Content Service (8082)

#### 1. Создание контента
```bash
curl -X POST http://localhost:8082/api/v1/content \
  -H "Content-Type: application/json" \
  -d '{
    "organizationId": 1,
    "contentData": {
      "h1": "Добро пожаловать",
      "title": "Тестовый лендинг",
      "description": "Описание лендинга",
      "aboutUs": "Информация о компании",
      "phone": "+79001234567",
      "email": "test@example.com",
      "address": "г. Москва, ул. Тестовая, д. 1",
      "workingHours": "Пн-Пт: 9:00-18:00"
    }
  }'
```

#### 2. Создание товара
```bash
curl -X POST http://localhost:8082/api/v1/products \
  -H "Content-Type: application/json" \
  -d '{
    "organizationId": 1,
    "name": "Тестовый товар",
    "description": "Описание тестового товара",
    "price": 10000.00,
    "category": "Товары",
    "isActive": true
  }'
```

#### 3. Создание акции
```bash
curl -X POST http://localhost:8082/api/v1/promotions \
  -H "Content-Type: application/json" \
  -d '{
    "organizationId": 1,
    "title": "Специальное предложение",
    "description": "Скидка 20% на все товары",
    "validFrom": "2025-11-01T00:00:00",
    "validTo": "2025-12-31T23:59:59",
    "isActive": true
  }'
```

### Template Service (8083)

#### 1. Создание шаблона
```bash
curl -X POST http://localhost:8083/api/v1/templates \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Базовый шаблон",
    "description": "Простой шаблон лендинга",
    "htmlStructure": "<!DOCTYPE html><html>...</html>",
    "cssStyles": "body { margin: 0; }",
    "jsScripts": "console.log(\"Hello\");",
    "isActive": true
  }'
```

#### 2. Рендеринг шаблона
```bash
curl "http://localhost:8083/api/v1/templates/1/render?organizationId=1"
```

### Order Service (8086)

Используйте автоматический скрипт:
```bash
./test-order-service.sh
```

Или выполните тесты вручную (см. раздел ниже).

---

## 🛒 Тестирование Order Service

### Автоматическое тестирование

```bash
./test-order-service.sh
```

Скрипт выполнит:
1. ✅ Создание заказа
2. ✅ Получение заказа по ID
3. ✅ Обновление статуса заказа
4. ✅ Получение заказов организации
5. ✅ Получение заказов по статусу
6. ✅ Проверку через API Gateway
7. ✅ Валидацию входных данных

### Ручное тестирование

#### 1. Создание заказа
```bash
curl -X POST http://localhost:8086/api/v1/orders \
  -H "Content-Type: application/json" \
  -d '{
    "organizationId": 1,
    "landingId": 1,
    "customerName": "Иван Иванов",
    "customerPhone": "+79001234567",
    "customerEmail": "ivan@example.com",
    "deliveryAddress": "г. Москва, ул. Ленина, д. 1",
    "comment": "Доставка с 10:00 до 18:00",
    "items": [
      {
        "productId": 1,
        "quantity": 2
      }
    ]
  }'
```

Ожидаемый ответ (HTTP 201):
```json
{
  "id": 1,
  "organizationId": 1,
  "landingId": 1,
  "customerName": "Иван Иванов",
  "customerPhone": "+79001234567",
  "customerEmail": "ivan@example.com",
  "deliveryAddress": "г. Москва, ул. Ленина, д. 1",
  "comment": "Доставка с 10:00 до 18:00",
  "totalAmount": 20000.00,
  "status": "NEW",
  "createdAt": "2025-11-03T22:00:00",
  "updatedAt": "2025-11-03T22:00:00",
  "items": [
    {
      "id": 1,
      "orderId": 1,
      "productId": 1,
      "productName": "Тестовый товар",
      "quantity": 2,
      "price": 10000.00,
      "totalPrice": 20000.00
    }
  ]
}
```

#### 2. Получение заказа
```bash
curl http://localhost:8086/api/v1/orders/1
```

#### 3. Обновление статуса
```bash
curl -X PATCH "http://localhost:8086/api/v1/orders/1/status?status=PROCESSING"
```

#### 4. Получение заказов организации
```bash
curl "http://localhost:8086/api/v1/orders/organization/1?page=0&size=10"
```

#### 5. Получение заказов по статусу
```bash
curl "http://localhost:8086/api/v1/orders/status/NEW?page=0&size=10"
```

#### 6. Отмена заказа
```bash
curl -X POST http://localhost:8086/api/v1/orders/1/cancel
```

#### 7. Удаление заказа
```bash
curl -X DELETE http://localhost:8086/api/v1/orders/1
```

---

## 🌐 Тестирование формы заказа

### Подготовка

1. Убедитесь, что все сервисы запущены
2. Создайте тестовые данные (организация, контент, товары)
3. Создайте шаблон с формой заказа

### Тестирование в браузере

#### 1. Открытие лендинга
```
http://localhost:8083/api/v1/templates/1/render?organizationId=1
```

#### 2. Проверка функциональности

**Добавление товара в корзину:**
- Нажмите кнопку "Добавить в корзину" на карточке товара
- Проверьте, что счетчик в header увеличился
- Проверьте уведомление "Товар добавлен в корзину"

**Открытие корзины:**
- Нажмите кнопку "Корзина" в header
- Проверьте, что модальное окно открылось
- Проверьте, что товары отображаются корректно

**Изменение количества:**
- Нажмите кнопку "+" или "-"
- Проверьте, что количество изменилось
- Проверьте, что общая сумма пересчиталась

**Удаление товара:**
- Нажмите кнопку "×" на товаре
- Проверьте, что товар удалился из корзины

**Заполнение формы:**
- Введите имя (обязательно)
- Введите телефон (обязательно, форматируется автоматически)
- Введите email (опционально, валидация формата)
- Введите адрес доставки (опционально)
- Введите комментарий (опционально)

**Отправка заказа:**
- Нажмите кнопку "Оформить заказ"
- Проверьте, что кнопка заблокировалась с текстом "Отправка..."
- Проверьте уведомление об успехе
- Проверьте, что корзина очистилась
- Проверьте, что модальное окно закрылось

#### 3. Проверка валидации

**Пустое имя:**
- Оставьте поле "Имя" пустым
- Нажмите "Оформить заказ"
- Проверьте сообщение об ошибке

**Некорректный телефон:**
- Введите неполный номер телефона
- Нажмите "Оформить заказ"
- Проверьте сообщение об ошибке

**Некорректный email:**
- Введите некорректный email (без @)
- Нажмите "Оформить заказ"
- Проверьте сообщение об ошибке

**Пустая корзина:**
- Удалите все товары из корзины
- Нажмите "Оформить заказ"
- Проверьте уведомление "Корзина пуста"

---

## 📱 Тестирование на мобильных устройствах

### Эмуляция в браузере

#### Chrome DevTools
1. Откройте DevTools (F12)
2. Нажмите Toggle Device Toolbar (Ctrl+Shift+M)
3. Выберите устройство:
   - iPhone 12 Pro (390x844)
   - Samsung Galaxy S20 (360x800)
   - iPad Pro (1024x1366)

#### Проверяемые аспекты

**Адаптивность:**
- ✅ Корректное отображение на разных разрешениях
- ✅ Читаемость текста
- ✅ Размер кнопок (минимум 44x44px)
- ✅ Отступы и padding

**Модальное окно:**
- ✅ Занимает весь экран на мобильных
- ✅ Скроллится корректно
- ✅ Закрывается по свайпу вниз (опционально)

**Форма заказа:**
- ✅ Поля ввода достаточно большие
- ✅ Клавиатура не перекрывает поля
- ✅ Автофокус на первом поле
- ✅ Кнопки легко нажимаются

**Корзина:**
- ✅ Товары отображаются в колонку
- ✅ Кнопки управления количеством удобные
- ✅ Общая сумма видна без скролла

### Тестирование на реальных устройствах

#### Подключение по локальной сети

1. Узнайте IP адрес компьютера:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

2. Откройте на мобильном устройстве:
```
http://[YOUR_IP]:8083/api/v1/templates/1/render?organizationId=1
```

Например:
```
http://192.168.1.100:8083/api/v1/templates/1/render?organizationId=1
```

#### Проверка на iOS (Safari)

- ✅ Корректное отображение
- ✅ Работа touch событий
- ✅ Форматирование телефона
- ✅ Автозаполнение полей
- ✅ Отправка формы

#### Проверка на Android (Chrome)

- ✅ Корректное отображение
- ✅ Работа touch событий
- ✅ Форматирование телефона
- ✅ Автозаполнение полей
- ✅ Отправка формы

---

## 📲 Проверка уведомлений Telegram

### Настройка Telegram Bot

#### 1. Создание бота

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте команду `/newbot`
4. Следуйте инструкциям
5. Сохраните токен бота

#### 2. Получение Chat ID

1. Найдите бота @userinfobot
2. Отправьте любое сообщение
3. Скопируйте ваш Chat ID

#### 3. Настройка Integration Service

Обновите `integration-service/src/main/resources/application.yml`:

```yaml
telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:YOUR_BOT_TOKEN}
    chat-id: ${TELEGRAM_CHAT_ID:YOUR_CHAT_ID}
```

Или установите переменные окружения:
```bash
export TELEGRAM_BOT_TOKEN="your_bot_token"
export TELEGRAM_CHAT_ID="your_chat_id"
```

### Тестирование уведомлений

#### 1. Создание заказа

Создайте заказ через API или форму заказа.

#### 2. Проверка Telegram

В течение нескольких секунд вы должны получить уведомление:

```
🛒 Новый заказ #1

👤 Клиент: Иван Иванов
📞 Телефон: +79001234567
💰 Сумма: 20000.00 руб.
📝 Комментарий: Доставка с 10:00 до 18:00
```

#### 3. Проверка логов

Если уведомление не пришло, проверьте логи:

```bash
# Логи Integration Service
tail -f logs/Integration\ Service.log

# Логи Order Service
tail -f logs/Order\ Service.log
```

Ищите строки:
```
Sending Telegram notification for order 1
Telegram notification sent for order 1
```

Или ошибки:
```
Failed to send Telegram notification for order 1
```

---

## 🐛 Troubleshooting

### Проблема: Сервис не запускается

**Симптомы:**
- Ошибка при запуске JAR файла
- Порт уже занят
- Connection refused

**Решения:**

1. Проверьте, что порт свободен:
```bash
lsof -i :8081  # Замените на нужный порт
```

2. Убейте процесс на порту:
```bash
kill -9 $(lsof -t -i:8081)
```

3. Проверьте логи:
```bash
tail -f logs/Landing\ Service.log
```

4. Проверьте Java версию:
```bash
java -version
```

### Проблема: База данных недоступна

**Симптомы:**
- Connection refused to PostgreSQL
- Unable to acquire JDBC Connection

**Решения:**

1. Проверьте статус Docker:
```bash
docker ps
```

2. Проверьте логи PostgreSQL:
```bash
docker logs klassifikator-postgres
```

3. Перезапустите PostgreSQL:
```bash
docker compose restart postgres
```

4. Проверьте подключение:
```bash
docker exec klassifikator-postgres pg_isready -U klassifikator_user
```

### Проблема: Redis недоступен

**Симптомы:**
- Unable to connect to Redis
- Connection refused to Redis

**Решения:**

1. Проверьте статус Redis:
```bash
docker ps | grep redis
```

2. Проверьте подключение:
```bash
docker exec klassifikator-redis redis-cli ping
```

3. Перезапустите Redis:
```bash
docker compose restart redis
```

### Проблема: Заказ не создается

**Симптомы:**
- HTTP 400 Bad Request
- HTTP 500 Internal Server Error
- Validation errors

**Решения:**

1. Проверьте, что товары существуют:
```bash
curl http://localhost:8082/api/v1/products
```

2. Проверьте, что организация существует:
```bash
curl http://localhost:8081/api/v1/organizations/1
```

3. Проверьте формат данных заказа

4. Проверьте логи Order Service:
```bash
tail -f logs/Order\ Service.log
```

### Проблема: Уведомления Telegram не приходят

**Симптомы:**
- Заказ создается, но уведомление не приходит
- Ошибки в логах Integration Service

**Решения:**

1. Проверьте токен бота и Chat ID

2. Проверьте, что Integration Service запущен:
```bash
curl http://localhost:8085/actuator/health
```

3. Проверьте логи:
```bash
tail -f logs/Integration\ Service.log
```

4. Проверьте доступность Telegram API:
```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

### Проблема: Форма заказа не работает

**Симптомы:**
- JavaScript ошибки в консоли
- Кнопки не реагируют
- Форма не отправляется

**Решения:**

1. Откройте консоль браузера (F12)

2. Проверьте ошибки JavaScript

3. Проверьте, что файлы подключены:
```html
<link rel="stylesheet" href="order-form.css">
<script src="order-form.js"></script>
```

4. Проверьте meta-теги:
```html
<meta name="organization-id" content="1">
<meta name="landing-id" content="1">
```

5. Проверьте доступность API:
```bash
curl http://localhost:8086/api/v1/orders
```

---

## 📊 Чеклист тестирования

### Инфраструктура
- [ ] PostgreSQL запущен и доступен
- [ ] Redis запущен и доступен
- [ ] MinIO запущен и доступен
- [ ] Все порты свободны

### Микросервисы
- [ ] Landing Service (8081) запущен
- [ ] Content Service (8082) запущен
- [ ] Template Service (8083) запущен
- [ ] Media Service (8084) запущен
- [ ] Integration Service (8085) запущен
- [ ] Order Service (8086) запущен
- [ ] API Gateway (8080) запущен

### Функциональность
- [ ] Создание организации работает
- [ ] Создание контента работает
- [ ] Создание товаров работает
- [ ] Создание акций работает
- [ ] Создание шаблонов работает
- [ ] Рендеринг шаблонов работает
- [ ] Создание заказов работает
- [ ] Получение заказов работает
- [ ] Обновление статуса работает

### Форма заказа
- [ ] Добавление товаров в корзину
- [ ] Изменение количества товаров
- [ ] Удаление товаров из корзины
- [ ] Расчет общей суммы
- [ ] Валидация формы
- [ ] Форматирование телефона
- [ ] Отправка заказа
- [ ] Уведомления работают

### Мобильные устройства
- [ ] Адаптивный дизайн на iPhone
- [ ] Адаптивный дизайн на Android
- [ ] Адаптивный дизайн на iPad
- [ ] Touch события работают
- [ ] Клавиатура не перекрывает поля

### Telegram
- [ ] Бот создан и настроен
- [ ] Уведомления приходят
- [ ] Формат уведомлений корректный

---

## 📝 Отчет о тестировании

После завершения тестирования заполните отчет:

### Дата тестирования
`2025-11-03`

### Окружение
- OS: macOS / Linux / Windows
- Java: версия
- Docker: версия
- Браузер: Chrome / Safari / Firefox

### Результаты

#### Успешные тесты
- Список успешно пройденных тестов

#### Проваленные тесты
- Список проваленных тестов с описанием проблем

#### Найденные баги
- Описание багов с шагами воспроизведения

#### Рекомендации
- Предложения по улучшению

---

**Автор**: AI Assistant  
**Дата последнего обновления**: 2025-11-03

