# 🎨 Руководство по использованию шаблона Landing-Basic

## 📋 Содержание
- [Обзор](#обзор)
- [Возможности](#возможности)
- [Структура](#структура)
- [Использование корзины](#использование-корзины)
- [Форма заказа](#форма-заказа)
- [Мобильная адаптация](#мобильная-адаптация)
- [Интеграция с API](#интеграция-с-api)

---

## 🎯 Обзор

**landing-basic** - это полнофункциональный шаблон лендинга с интегрированной системой заказов, корзиной товаров и формой оформления заказа.

### Ключевые особенности:
- ✅ Адаптивный дизайн (Desktop/Tablet/Mobile)
- ✅ Shopping Cart (корзина товаров)
- ✅ Форма оформления заказа
- ✅ Интеграция с Order Service API
- ✅ Сохранение корзины в localStorage
- ✅ Telegram уведомления о новых заказах
- ✅ Yandex Maps и Metrika
- ✅ SEO-оптимизация

---

## 🚀 Возможности

### 1. **Корзина товаров**
- Фиксированная кнопка корзины (правый нижний угол)
- Счётчик товаров в корзине
- Боковая панель корзины
- Управление количеством товаров (+/-)
- Удаление товаров
- Автоматический расчёт суммы

### 2. **Каталог товаров**
- Карточки товаров с изображениями
- Название, описание, категория
- Цена
- Кнопка "В корзину"
- Поддержка изображений из S3

### 3. **Форма заказа**
- Модальное окно оформления
- Контактные данные (имя, телефон, email)
- Адрес доставки
- Комментарий к заказу
- Сводка заказа
- Валидация полей
- Форматирование телефона

### 4. **Акции и предложения**
- Карточки акций
- Описание акций
- Срок действия
- Изображения (опционально)

---

## 📁 Структура

### HTML (index.html)
```
<!DOCTYPE html>
<html>
<head>
  - Meta теги (SEO)
  - Подключение стилей (styles.css, order-form.css)
</head>
<body>
  - Кнопка корзины (fixed)
  - Панель корзины
  - Header (навигация)
  - Hero Section (главный блок)
  - About Section (о нас)
  - Promotions Section (акции)
  - Products Section (каталог)
  - Contacts Section (контакты + карта)
  - Footer
  - Order Modal (модальное окно заказа)
  - Notification (уведомления)
  - Scripts
</body>
</html>
```

### CSS
1. **styles.css** - основные стили
   - Header
   - Hero
   - Sections
   - Products Grid
   - Promotions
   - Contacts
   - Footer
   - Responsive (@media queries)

2. **order-form.css** - стили корзины и формы
   - Shopping Cart Button
   - Cart Panel
   - Modal Window
   - Order Form
   - Notifications
   - Mobile Responsive

### JavaScript (order-form.js)
```javascript
// Функции корзины
- initCart()
- addToCart(productId, name, price)
- removeFromCart(productId)
- updateQuantity(productId, delta)
- calculateTotal()
- updateCart()

// Функции модального окна
- openOrderModal()
- closeOrderModal()
- updateOrderSummary()

// Отправка заказа
- submitOrder(event)

// Утилиты
- formatPhoneNumber(input)
- showNotification(message, type)
- formatPrice(price)
```

---

## 🛒 Использование корзины

### Добавление товара в корзину

В HTML:
```html
<button 
    class="btn btn-primary btn-add-to-cart" 
    onclick="addToCart({{id}}, '{{name}}', {{price}})"
    data-product-id="{{id}}">
    В корзину
</button>
```

JavaScript автоматически:
1. Добавляет товар в массив `cart`
2. Сохраняет корзину в `localStorage`
3. Обновляет UI (счётчик, панель)
4. Показывает уведомление

### Панель корзины

Открывается по клику на кнопку корзины (🛒):
```javascript
function toggleCart() {
    const cartPanel = document.getElementById('cartPanel');
    cartPanel.classList.toggle('active');
}
```

### Структура данных корзины

```javascript
cart = [
    {
        productId: 19,
        productName: "Стол офисный 'Лидер'",
        price: 1500,
        quantity: 2
    },
    // ...
]
```

---

## 📝 Форма заказа

### Открытие формы

```javascript
function openOrderModal() {
    // Проверка корзины
    if (cart.length === 0) {
        showNotification('Корзина пуста', 'warning');
        return;
    }
    
    // Открытие модального окна
    const modal = document.getElementById('orderModal');
    modal.classList.add('active');
    
    // Обновление сводки заказа
    updateOrderSummary();
}
```

### Отправка заказа

```javascript
async function submitOrder(event) {
    event.preventDefault();
    
    // Подготовка данных
    const orderData = {
        organizationId: window.ORGANIZATION_ID,
        landingId: window.LANDING_ID,
        customerName: "...",
        customerPhone: "...",
        customerEmail: "...",
        deliveryAddress: "...",
        comment: "...",
        items: [
            {productId: 19, quantity: 2},
            // ...
        ]
    };
    
    // Отправка на API
    const response = await fetch(`${window.API_URL}/orders`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(orderData)
    });
    
    // Обработка ответа
    // ...
}
```

---

## 📱 Мобильная адаптация

### Breakpoints

```css
/* Tablet */
@media (max-width: 768px) {
    - Скрытие навигации
    - Адаптация grid (1 колонка)
    - Уменьшение отступов
    - Полноэкранная корзина
}

/* Mobile */
@media (max-width: 480px) {
    - Уменьшение шрифтов
    - Адаптация карточек
    - Упрощение UI
}
```

### Тестирование

1. **Chrome DevTools**
   - F12 → Device Toolbar (Ctrl+Shift+M)
   - Выбрать устройство (iPhone, iPad, etc.)

2. **Real Device Testing**
   - Открыть лендинг на реальном устройстве
   - Проверить touch interactions
   - Проверить форму ввода

---

## 🔌 Интеграция с API

### Конфигурация

В HTML перед подключением `order-form.js`:

```html
<script>
    window.ORGANIZATION_ID = {{organizationId}};
    window.LANDING_ID = {{landingId}};
    window.API_URL = 'http://localhost:8080/api/v1';
</script>
```

### API Endpoints

1. **Создание заказа**
   ```
   POST /api/v1/orders
   Content-Type: application/json
   
   {
     "organizationId": 1,
     "landingId": 2,
     "customerName": "Иван Иванов",
     "customerPhone": "+7 (900) 123-45-67",
     "customerEmail": "ivan@example.com",
     "deliveryAddress": "г. Волжский, ул. Мира, 10",
     "comment": "Доставка после 18:00",
     "items": [
       {"productId": 19, "quantity": 2}
     ]
   }
   ```

2. **Ответ**
   ```json
   {
     "id": 12,
     "organizationId": 1,
     "landingId": 2,
     "customerName": "Иван Иванов",
     "totalAmount": 3000.0,
     "status": "PENDING",
     "items": [
       {
         "id": 23,
         "productId": 19,
         "productName": "Стол офисный 'Лидер'",
         "quantity": 2,
         "price": 1500.0,
         "totalPrice": 3000.0
       }
     ],
     "createdAt": "2025-11-03T16:41:29.544"
   }
   ```

### Telegram уведомления

При успешном создании заказа:
1. Order Service отправляет данные в Integration Service
2. Integration Service форматирует сообщение
3. Telegram Bot отправляет уведомление

Формат уведомления:
```
🛒 НОВЫЙ ЗАКАЗ

🏢 Организация: Модернисимо
🌐 Домен: modernissimo.volzhck.ru

📋 Заказ #12
👤 Клиент: Иван Иванов
📞 Телефон: +7 (900) 123-45-67
✉️ Email: ivan@example.com
🚚 Адрес: г. Волжский, ул. Мира, 10

💰 Сумма: 3000.0 руб

📦 Товары:
  • Стол офисный "Лидер" × 2 = 3000.0 руб

💬 Комментарий: Доставка после 18:00

🕐 03.11.2025 16:41
```

---

## 📊 Analytics & Tracking

### Yandex Metrika

Автоматическая отправка события при оформлении заказа:

```javascript
if (window.ym && window.yandexMetrikaId) {
    ym(window.yandexMetrikaId, 'reachGoal', 'purchase', {
        order_price: calculateTotal(),
        currency: 'RUB'
    });
}
```

### Google Analytics (опционально)

Добавьте в HTML:
```html
<script>
    gtag('event', 'purchase', {
        transaction_id: orderId,
        value: totalAmount,
        currency: 'RUB'
    });
</script>
```

---

## 🎨 Кастомизация

### Цвета

В `styles.css`:
```css
:root {
    --primary-color: #2563eb;      /* Основной цвет */
    --primary-hover: #1d4ed8;      /* При наведении */
    --secondary-color: #64748b;    /* Вторичный цвет */
    --success-color: #10b981;      /* Успех */
    --danger-color: #ef4444;       /* Ошибка */
}
```

### Шрифты

```css
:root {
    --font-main: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
}
```

### Логотип

Замените в HTML:
```html
<div class="logo">
    <img src="{{logoUrl}}" alt="{{organizationName}}">
</div>
```

---

## ✅ Чеклист перед запуском

- [ ] Проверить данные организации (название, телефон, адрес)
- [ ] Добавить товары с изображениями
- [ ] Настроить Yandex Maps (координаты)
- [ ] Настроить Yandex Metrika (ID счётчика)
- [ ] Настроить Telegram Bot (token, chat-id)
- [ ] Протестировать на Desktop
- [ ] Протестировать на Mobile
- [ ] Проверить создание заказа
- [ ] Проверить Telegram уведомления
- [ ] Проверить сохранение корзины
- [ ] Настроить домен

---

## 🐛 Troubleshooting

### Корзина не сохраняется
- Проверьте `localStorage` в DevTools
- Очистите кэш браузера

### Заказ не отправляется
- Проверьте console.log в DevTools
- Проверьте Network tab (статус запроса)
- Проверьте работу Order Service

### Telegram уведомления не приходят
- Проверьте настройки Telegram Bot
- Проверьте Integration Service logs
- Проверьте token и chat-id

### Изображения не загружаются
- Проверьте URL изображений
- Проверьте CORS настройки S3
- Проверьте публичный доступ к bucket

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервисов
2. Проверьте browser console
3. Проверьте network requests

---

**Версия**: 1.0  
**Дата создания**: 2025-11-03  
**Последнее обновление**: 2025-11-03

