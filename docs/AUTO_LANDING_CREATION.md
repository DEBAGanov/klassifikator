# Автоматическое создание лендингов через Google Sheets

## 📋 Обзор

Система автоматически создаёт лендинги для организаций из Google Sheets. При добавлении новой строки в таблицу, после синхронизации создаётся:
- **Organization** (организация)
- **Landing** (лендинг с доменом)
- **Content** (контент со всеми данными)
- **Products** (товары)
- **Promotions** (акции)

## 🚀 Как это работает

### Шаг 1: Подготовка домена

1. Откройте панель Timeweb Cloud: https://timeweb.cloud/my/domains
2. Создайте поддомен вручную:
   - Перейдите в домен `volzhck.ru`
   - Нажмите "Создать поддомен"
   - Укажите поддомен: `sushi-era` (для `sushi-era.volzhck.ru`)
   - Выберите сервер или IP-адрес (ваш облачный сервер)
   - Сохраните

3. **SSL сертификат выдастся автоматически** от Timeweb Cloud

### Шаг 2: Добавление организации в Google Sheets

1. Откройте таблицу: https://docs.google.com/spreadsheets/d/1KS2TOS5ZKxONDmUaVoiwb3tyu3Y1DlGQaME2KM4vItQ/edit?gid=1925574043#gid=1925574043

2. Добавьте новую строку с данными:
   - **Название**: Название организации (например, "Суши-Эра")
   - **Домен**: Полный домен (например, "sushi-era.volzhck.ru")
   - **Категория**: Категория бизнеса (например, "Общепит")
   - **Тип**: Тип организации (например, "Ресторан")
   - **Телефон**: Номер телефона
   - **Адрес**: Физический адрес
   - **Режим работы**: Время работы
   - **Title**: SEO заголовок
   - **Description**: SEO описание
   - **H1**: Главный заголовок на лендинге
   - ... и остальные поля (товары, акции, фото и т.д.)

### Шаг 3: Запуск синхронизации

#### Вариант A: Ручная синхронизация через API

```bash
curl -X POST "http://localhost:8085/api/v1/integration/google-sheets/sync-all?sheetName=Organizations" \
  -H "Content-Type: application/json"
```

**Ответ:**
```json
{
  "total": 10,
  "created": 1,
  "updated": 0,
  "failed": 0,
  "errors": [],
  "status": "SUCCESS",
  "timestamp": "2025-11-05T01:23:37.354724"
}
```

#### Вариант B: Автоматическая синхронизация (по расписанию)

Синхронизация запускается автоматически каждые 5 минут (настраивается в `application.yml`):

```yaml
klassifikator:
  google-sheets:
    sync-interval-minutes: 5
```

### Шаг 4: Проверка результата

1. **Проверить, что организация создалась:**
```bash
curl http://localhost:8081/api/v1/organizations
```

2. **Проверить, что лендинг создался:**
```bash
curl http://localhost:8081/api/v1/landings
```

3. **Открыть лендинг в браузере:**
```
http://sushi-era.volzhck.ru
```

или через прокси (локально):
```
http://localhost:8084/sushi-era.volzhck.ru
```

## 📊 Что происходит внутри

1. **Integration Service** читает все строки из Google Sheets
2. Для каждой строки:
   - Извлекает домен из столбца "Домен"
   - Проверяет, существует ли Landing с таким доменом
   - **Если НЕ существует:**
     - Создаёт Organization через `POST /api/v1/organizations`
     - Создаёт Landing через `POST /api/v1/landings`
     - Создаёт Content, Products, Promotions через Content Service
   - **Если существует:**
     - Обновляет только Content, Products, Promotions

3. Лендинг сразу доступен по URL с шаблоном `landing-basic`

## 🔧 Настройки

### Указать другой spreadsheetId

Если у вас несколько таблиц:

```bash
curl -X POST "http://localhost:8085/api/v1/integration/google-sheets/sync-all?spreadsheetId=YOUR_SHEET_ID&sheetName=Organizations"
```

### Указать другое имя листа

Если лист называется не "Organizations":

```bash
curl -X POST "http://localhost:8085/api/v1/integration/google-sheets/sync-all?sheetName=MySheetName"
```

## ⚠️ Важные моменты

### 1. Домены второго уровня

- Создавайте поддомены **вручную** в Timeweb Cloud
- Формат: `subdomain.volzhck.ru` (например, `sushi-era.volzhck.ru`)
- SSL сертификат выдаётся автоматически Timeweb Cloud

### 2. Уникальность домена

- Каждый домен должен быть уникальным
- Если домен уже существует, организация **обновится**, а не создастся заново

### 3. Обязательные поля

Минимум для создания лендинга:
- **Название** - имя организации
- **Домен** - полный домен (например, `company.volzhck.ru`)

Все остальные поля опциональны.

### 4. Template ID

Все лендинги автоматически создаются с `templateId = 1` (шаблон `landing-basic`).

## 📝 Структура Google Sheets

### Обязательные столбцы:

| Столбец | Описание | Пример |
|---------|----------|--------|
| Название | Имя организации | "Суши-Эра" |
| Домен | Полный домен | "sushi-era.volzhck.ru" |

### Рекомендуемые столбцы:

| Столбец | Описание | Пример |
|---------|----------|--------|
| Категория | Категория бизнеса | "Общепит" |
| Тип | Тип организации | "Ресторан" |
| Телефон | Номер телефона | "+7 (123) 456-78-90" |
| Адрес | Физический адрес | "г. Волжский, ул. Ленина, 1" |
| Режим работы | Время работы | "Пн-Вс: 10:00 - 22:00" |
| Title | SEO заголовок | "Суши-Эра - доставка суши в Волжском" |
| Description | SEO описание | "Свежие роллы и суши с доставкой..." |
| H1 | Главный заголовок | "Суши-Эра" |
| О нас | Описание компании | "Мы готовим лучшие роллы..." |
| Особенности | Список фишек через запятую | "Бесплатная доставка, Акции каждый день, Свежие продукты" |
| Товары/услуги | Товары в формате JSON | см. раздел "Формат данных" |
| Акции | Акции в формате JSON | см. раздел "Формат данных" |
| Фото компании | URL фотографий через запятую | "https://example.com/photo1.jpg, https://example.com/photo2.jpg" |

### Формат данных

#### Товары/услуги (JSON):
```json
[
  {"name": "Калифорния", "price": 450, "description": "Классический ролл", "category": "Роллы", "image": "url"},
  {"name": "Филадельфия", "price": 550, "description": "С лососем и сыром", "category": "Роллы", "image": "url"}
]
```

#### Акции (JSON):
```json
[
  {"title": "Скидка 20%", "description": "На первый заказ", "image": "url"},
  {"title": "Бесплатная доставка", "description": "При заказе от 1000 руб", "image": "url"}
]
```

## 🎯 Примеры использования

### Создать 10 лендингов для разных компаний

1. Заполните 10 строк в Google Sheets
2. Создайте 10 поддоменов в Timeweb Cloud:
   - `company1.volzhck.ru`
   - `company2.volzhck.ru`
   - ...
   - `company10.volzhck.ru`
3. Запустите синхронизацию:
```bash
curl -X POST "http://localhost:8085/api/v1/integration/google-sheets/sync-all"
```

Результат: 10 лендингов созданы и доступны!

### Обновить контент существующего лендинга

1. Измените данные в Google Sheets (например, обновите товары)
2. Запустите синхронизацию:
```bash
curl -X POST "http://localhost:8085/api/v1/integration/google-sheets/sync-all"
```

Результат: Контент обновлён, организация и лендинг остались прежними.

## 🐛 Отладка

### Проверить логи синхронизации:

```bash
tail -f logs/integration-service.log | grep "Processing organization"
```

### Проверить созданные организации:

```bash
curl http://localhost:8081/api/v1/organizations | jq '.[] | {id, name, category}'
```

### Проверить созданные лендинги:

```bash
curl http://localhost:8081/api/v1/landings | jq '.[] | {id, domain, organizationId, status}'
```

### Удалить тестовые данные:

```bash
# Удалить лендинг
curl -X DELETE http://localhost:8081/api/v1/landings/5

# Удалить организацию
curl -X DELETE http://localhost:8081/api/v1/organizations/4
```

## 📚 API Reference

### POST /api/v1/integration/google-sheets/sync-all

Массовая синхронизация всех организаций из Google Sheets.

**Parameters:**
- `spreadsheetId` (optional) - ID Google таблицы
- `sheetName` (default: "Organizations") - Имя листа

**Response:**
```json
{
  "total": 10,          // Всего строк прочитано
  "created": 5,         // Новых организаций создано
  "updated": 3,         // Существующих обновлено
  "failed": 2,          // Ошибок
  "errors": [...],      // Список ошибок
  "status": "SUCCESS",
  "timestamp": "2025-11-05T01:23:37.354724"
}
```

## 🎉 Готово!

Теперь вы можете создавать сотни лендингов просто добавляя строки в Google Sheets! 🚀

---

**Версия**: 1.0  
**Дата создания**: 2025-11-05  
**Последнее обновление**: 2025-11-05

